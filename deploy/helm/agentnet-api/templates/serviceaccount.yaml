{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agentnet-api.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "agentnet-api.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
