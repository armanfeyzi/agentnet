{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agentnet-web.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "agentnet-web.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
