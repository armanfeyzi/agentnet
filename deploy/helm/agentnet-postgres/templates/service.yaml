apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentnet-postgres.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "agentnet-postgres.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "agentnet-postgres.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: postgres
      port: {{ .Values.service.port }}
      targetPort: postgres
